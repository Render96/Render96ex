name: Test Workflow

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print Hello
        run: |
          echo "➜ Workflow is running!"
          echo "Date: $(date)"
